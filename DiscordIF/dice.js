'use strict';

class Dice 
{
	constructor(number, type) 
	{
		this.number = number;
		this.type = type;
	}
	
	{
			total_roll += roll;
			if (roll <= lowest) {
				lowest = roll;
			}
		}
		total_roll -= lowest;
		return total_roll;
	}
}
